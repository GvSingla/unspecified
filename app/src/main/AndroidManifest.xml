<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gvsingla.projectandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DashboardActivity" />
        <activity android:name=".SchoolProjects" />
        <activity android:name=".CollegeProjects" />
        <activity android:name=".ArtAndCraft" />
        <activity android:name=".Paintings" />
        <activity android:name=".EduBooks" />
        <activity android:name=".Novels" />
        <activity android:name=".Stationary" />
        <activity android:name=".Settings"></activity>
    </application>

</manifest>